version: "3.9" 

name: "test"

services:
  react:
    image: react
    build:  
        dockerfile: ./client/Dockerfile
    ports:
     - "3000:80"
  flask:
    image: flask
    build:
        dockerfile: ./server/Dockerfile
    ports:
      - "5000:5000"




