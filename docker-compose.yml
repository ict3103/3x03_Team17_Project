version: "3.9" 

name: "3x03-Team17-debug"

services:
  react:
    image: react2
    build:  
        dockerfile: ./client/Dockerfile
    ports:
     - "3000:3000"
  flask:
    image: flask2
    build:
        dockerfile: ./server/Dockerfile
    ports:
      - "5000:80"




