pipeline{
    agent any

    stages{
        stage('build'){
            steps{
                echo "in build stage"
            }
        }

        stage("test"){
            steps{
                echo "in test stage"
            }
        }

        stage("deploy"){
            steps{
                echo "try to deploy application"
            }
        }
    }
}